

jc-colors = {
  "primary": {
    "base": #263238,
    "light": #4f5b62,
    "dark": #000a12,
    "contrast": white
  },
  "accent": {
    "base": rgb(240,147,43),
    "light": mix(white,rgb(240,147,43),20%),
    "dark": mix(black,rgb(240,147,43),20%),
    "contrast": #111
  },
  "neutral": {
    "base": #333,
    "light": rgb(83,91,104),
    "dark": #111,
    "contrast": white
  },
  "secondary": {
    "base": #407A52,
    "light": #98ee99,
    "dark": #338a3e,
    "contrast": #fff
  },
  "warn": {
    "base": #fb8c00,
    "light": #ffbd45,
    "dark": #c25e00,
    "contrast": black
  }
}

color-interval = 1%
get-color(name = "primary", variant = "base", alpha = 1)
  retval = null
  color-spectrum = jc-colors[name]
  retval = variant in color-spectrum ? color-spectrum[variant] : get-color(name)

  if typeof(variant) == "unit"
    color-base = variant < 0 ? black : white
    retval = mix(color-base, retval, abs(variant) * color-interval)
  if alpha
    retval = rgba(retval, alpha)
  retval